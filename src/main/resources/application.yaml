spring:
  application:
    name: tealk
  datasource:
    url: jdbc:mysql://localhost:3306/tealk
    username: root
    password: '12345678'
  jackson:
    date-format: yyyy-MM-dd
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

